<?php
include "db_connect.php";
session_start();

if(isset($_POST['login'])){
    $username = $_POST['username'];
    $password = $_POST['password'];

    $result = mysqli_query($conn,"SELECT * FROM users WHERE username='$username' AND password='$password'");

    if(mysqli_num_rows($result) > 0){

        $_SESSION['username'] = $username;

        if($username == "admin" && $password == "admin123"){
            header("Location: ADMIN.php");
            exit;
        }

        header("Location: LOSTT.php");
        exit;
    } else {
        echo "<script>alert('Invalid Login');</script>";
    }
}
?>

<!doctype html>
<html>
<head>
<meta charset="utf-8">
<title>LOGINN</title>
	<style>
		body{
		background-image: url(image/bg.jpg);
		background-attachment: fixed;
		background-repeat: no-repeat;
		background-position: center;
		background-size: cover;
		font-family: Arial, sans-serif;
	}

		.text-section {
		text-align: center;
		color: #000;
		font-size: 36px;
		text-decoration: none;
	}
		.text-section2 {
		text-align: center;
		color: #000;
		font-size: 14px;
		text-decoration: none;
	}

		.login-box {
		background-color: #f8f8c3;
		width: 320px;
		padding: 30px;
		border: 2px solid black;
		border-radius: 20px;
		text-align: center;
		margin: 50px auto;
		font-size: 18px;
	}

		.login-box label {
		display: block;
		text-align: left;
		margin-bottom: 5px;
		font-weight: bold;
		margin-left: 5px;
		font-size: 18px;
	}

		.login-box input {
		width: calc(100% - 22px);
		padding: 10px;
		border: 1px solid black;
		border-radius: 5px;
		font-size: 16px;
		margin-bottom: 15px;
		box-sizing: border-box;
	}
		
		button {
		background-color: white;
	    color: black;
	    border: 1px solid black;
		padding: 8px 20px;
		border-radius: 6px;
		font-size: 14px;
		cursor: pointer;
	}

		button:hover {
		background-color: #f2f2f2; 
	}
</style>
</head>

<body>
<!-- Header -->
<table width="1300" border="0">
	<tr>
		<td width="293"><img src="image/logoo.png" width="250" height="150" alt=""/></td>
		<td></td>
	</tr>
</table>
<main>
	<!-- Login Section -->
<table class="table-wrap" align="center">
	<tr>
		<td width="556" class="text-section">
			<strong>Welcome to I-Found</strong><br>
			<font size="4">
				We're here to help you quickly report lost belongings, search for found items, and reconnect with what matters most. 
				Our mission is to make finding and returning items easy, safe, and efficient for the UPTM community.<br>
				Happy searching and thank you for being part of I-Found!
			</font>
		</td>

		<td width="421" align="center">
			<div class="login-box">
    			<h2><u>LOGIN</u></h2>

				<form method="POST" action="">
    			<label>Username</label>
    			<input type="text" name="username" placeholder="Enter username">

    			<label>Password</label>
    			<input type="password" name="password" placeholder="Enter password">

    			<div>
        		<button type="submit" name="login">LOG IN</button>
        		<button class="button"><a href="SIGN_UP.php" class="text-section2">SIGN UP</a></button><br>
    			</div>
				</form>
			</div>

		</td>
	</tr>
</table>
</main>
</body>
</html>
